import { h } from 'preact';
import { Page, Navbar, Block, List, ListItem, Icon, Button } from 'preact-f7';


const Home = ({ reduxState }) => (
	<Page name="NotFound" >
		
		<Block text="Page not found!" />

	</Page>
);

export default Home;
